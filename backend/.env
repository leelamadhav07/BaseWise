APP_NAME=Basewise API
APP_VERSION=0.1.0
DEBUG=True
DATABASE_URL=postgresql://postgres.nvovpmyzxxermklmpvuk:Leelamadhav%402007@aws-1-ap-south-1.pooler.supabase.com:5432/postgres


# changed supabase Direct connection string to supabase session pooler connection string.
# because my own local network is ipv4 and supabase direct connection string is ipv6 so we need ipv4.
# so that is why we changed direct to session connection string (ipv4 to ipv4).