# Basewise Frontend (React)

Not built yet — this comes after the FastAPI backend has working
endpoints to call.

When we get here, this folder will contain a React app that:

- Lets a user type natural-language questions about their database
- Displays generated answers, plain-English explanations, and charts
- Talks to the FastAPI backend (`../backend/`) over HTTP — never
  directly to the database or to the `basewise` package
